# 📜 Chronicle Knights — 開発進捗レポート（C# / Godot 移行版）

> **生成日時**: 2026-06-10
> **対象コードベース**: `generated_csharp/`（Godot 4 / .NET 8 / C# 12）
> **規模**: 37 ファイル / 約 8,793 行
> **基準コミット**: `ae0c98d`（ジョブパッシブ結合解決を移植）
> **参照原典**: `packages/` + `apps/`（TypeScript 版 — 不変の参照、変更禁止）

---

## 📖 このドキュメントの読み方

このレポートは **2 種類の読者**を想定して書かれています。

- **🎮 ゲームや経営を見る人へ** … セクション 1〜3 を読めば「このゲームが何を目指し、いま何ができていて、何が足りないか」が技術知識ゼロでも分かります。
- **🛠 エンジニアへ** … セクション 2〜6 を読めば「設計の憲法・各システムの正確な計算式・次に何を実装すべきか」が 100% クリアになります。

各セクションの冒頭に **【ひとことで言うと】** を置いています。急ぐ場合はそこだけ拾い読みしてください。

---

# 1. イントロダクション ＆ 本作のコア思想

> **【ひとことで言うと】** 「一人の英雄」ではなく「血脈（一族）」を何世代も受け継いで戦う、時間が一気に流れる育成ストラテジー。1 回の戦いの裏で何年も時が過ぎ、英雄は老い、子は育ち、世代が入れ替わっていく。

## 1-1. 本作が目指す姿

**Chronicle Knights** は、名作『ヴィーナス＆ブレイブス』にインスパイアされた **世代交代型シミュレーション RPG** です。本リポジトリでは、これを **ハクスラ（ハック＆スラッシュ）/ ローグライト** の遺伝子と融合させた新世代の作品として、Web 技術（TypeScript）から **Godot 4 / .NET 8 / C#** へ全面移植しています。

プレイヤーは個々の英雄ではなく、**「旅団（大隊）」という一つの組織と、そこに連なる血脈の物語**を導きます。最強の剣士もやがて老いて引退（＝寿命による離脱）します。だからこそ、その子を産み育て、外様（血縁のない傭兵）を雇い入れ、何世代にもわたって旅団を存続させていく — それがこのゲームの根幹です。

## 1-2. コア思想：「1 世代 ＝ 時間軸 1 周」

本作の時間設計を理解する鍵が、この一文です。

```
   ┌──────── 1 世代（ゲームループ 1 周） ────────┐
   │                                             │
 年代記 ──▶ 旅団組合 ──▶ 大隊編成 ──▶ 戦闘 ──┐  │
 (予言を選ぶ)  (婚姻/スカウト)  (9名配置)  (決着)  │  │
   ▲                                          │  │
   └──────────── 年送り（数年が一気に流れる）◀┘  │
              ・全員が加齢する                     │
              ・寿命/戦死した者は永久離脱（完全ロスト）│
              ・収入が入る                          │
              ・次の予言が提示される                 │
   └─────────────────────────────────────────────┘
```

プレイヤーは年代記フェーズで **「予言」を 1 つ選びます**。予言には「この先 ◯ 年が流れる（タイムスキップ）」という年数が記されています。プレイヤーはその年数ぶんの未来を、**1 周のゲームループを通して戦い抜き**、ループの最後（戦闘 → 年代記への巻き戻り）で **その年数が一気に経過します**。

この瞬間に：

- 旅団員全員が選んだ年数ぶん歳を取り、
- 寿命に達した者・戦闘で死んだ者は **二度と戻らない（完全ロスト）**、
- 時の流れに応じた収入が入り、
- そして新たな未来（次の予言 3 つ）が提示される。

つまり **「Chronicle で選んだ年数＝この世代の長さ」**であり、ゲームの 1 周がそのまま 1 世代の生き様になります。30 回ループを回せば、それは旅団の 30 年史（クロニクル）そのものになる、という時間圧縮の美学が設計の中心にあります。

---

# 2. 開発憲法とアーキテクチャの基本構造

> **【ひとことで言うと】** このコードベースには「絶対に破ってはならない 3 つの掟」がある。それを徹底することで、バグが起きにくく・翻訳しやすく・テストしやすい「壊れない設計」を実現している。

## 2-1. 絶対に破ってはならない 3 大憲法

### 🏛 憲法 ①：日本語ハードコードの絶対禁止 — 「言葉」と「仕組み」の分離

**何が問題か（噛み砕いて）**: もしコードの中に「鉄壁騎士」「衛生兵」といった日本語を直接書いてしまうと、英語版を作る時にコード全体を書き換える羽目になり、誤訳・書き換えミスの温床になります。

**どう解決しているか**: コードは **ASCII の「キー（合言葉）」だけ**を扱います。実際の日本語テキストは `Config/localization_ja.json` という **辞書ファイルに完全分離**されています。

```
 コード内:  unit.FirstNameKey = "name-japanese-male-007"   ← ただの合言葉
                            │
                            ▼ 表示するとき NameResolver が辞書を引く
 JSON辞書:  "name-japanese-male-007": "タケル"            ← ここにだけ日本語
                            │
                            ▼
 画面表示:  タケル
```

これにより、**ゲームロジックは一切日本語を知らないまま動き**、将来の多言語化は JSON を 1 枚追加するだけで済みます。

> ⚠️ **例外として許容しているもの**: コード内の「コメント（説明書き）」と、開発者向けの「診断用エラーメッセージ」の日本語は許容されています（プレイヤーの目に触れないため）。

### 🏛 憲法 ②：不変性（イミュータブル）の徹底 — 「状態は書き換えず、作り直す」

**何が問題か（噛み砕いて）**: あるユニットの年齢を `unit.Age = 30` のように直接書き換えると、「いつ・どこで・誰が変えたのか」を追えなくなり、戦闘中に値が想定外に化ける『状態のバグ』が起きます。

**どう解決しているか**: すべてのデータは **`record`（不変レコード）**として定義され、一度作ったら**中身を絶対に書き換えません**。変更したいときは `with` 式で **新しいコピーを作る**だけです。

```csharp
// ❌ こうは絶対にしない（破壊的変更）
unit.Age = unit.Age + 4;

// ✅ 必ずこうする（新しいインスタンスを返す）
var olderUnit = unit.WithAgeProgress(4);   // 元の unit は無傷のまま、+4 歳の別個体が返る
```

リスト全体も `ImmutableList<Unit>` / `ImmutableArray<Prophecy>` を使い、**「全部まるごと差し替える」**スタイルで管理します。これにより「気づかぬうちに誰かが変えていた」が原理的に起こりません。

### 🏛 憲法 ③：常駐 SoT ＋ 単方向データフロー — 「真実は 1 か所だけ」

**何が問題か（噛み砕いて）**: 経済の残高・旅団名簿・予言の状態が複数の画面にバラバラに散らばっていると、画面 A と画面 B で残高がズレる、といった『真実の食い違い』が起きます。

**どう解決しているか**: ゲーム全体の状態は **`ChronicleGlobal`（常駐シングルトン）ただ一人**が握ります。これが **SoT（Single Source of Truth ＝ 唯一の真実）**です。UI（各画面）は状態を**絶対に直接書き換えられず**、必ず `ChronicleGlobal` の API を呼んでお願いし、結果を **シグナル（通知）で受け取って描き直す**だけです。

```
 ① UIが「これやって」と頼む（例: 予言を選んだ）
       │
       ▼
 ┌───────────────────────────────┐
 │  ChronicleGlobal （唯一の真実） │
 │   ・残高  ・名簿  ・予言        │
 └───────────────────────────────┘
       │ ② 内部で純粋ロジック(BattleManager等)を呼ぶ
       │ ③ 新しい不変レコードを受け取り「丸ごと差し替え」
       │ ④ 【ロックを外してから】シグナルを発火
       ▼
 ⑤ UIがシグナルを受けて、状態を読み直して再描画
```

**スレッド安全とシグナル発火の規律（重要）**:

- 状態の変更はすべて `lock(_stateLock)` の**排他ロック内**で行う（並列呼び出しでも壊れない）。
- シグナル（`EmitSignal`）は **必ずロックを解放した後**に発火する（ロック内発火はデッドロックの危険があるため厳禁）。
- **データ確定 → 通知**の順序を死守する。特に年送り時は「名簿 → 経済 → 予言 → フェーズ変更」の順で発火し、**画面が切り替わる前に新しいデータが必ず確定している**ことを保証する。

## 2-2. アーキテクチャ全景：「純粋な脳」と「Godot の身体」の分離

このコードベースの最も美しい点は、**ゲームのルール（脳）を Godot（身体）から完全に切り離している**ことです。

```
generated_csharp/
│
├── Core/  ★★★ 純粋ゲームロジック（Godot を一切知らない脳） ★★★
│   │       → Godot 無しで動く＝xUnit で単体テストできる
│   ├── GameFlow/      GamePhase.cs          フェーズ状態マシン
│   ├── Job/           JobData.cs            ジョブ定義の型
│   │                  JobMaster.cs          全 8 ジョブの能力値 SoT
│   ├── Managers/      PointsEconomy.cs      ポイント一元経済
│   │                  TimelineEngine.cs     予言タイムライン
│   │                  RosterLifecycle.cs    世代交代（加齢・離脱仕分け）
│   │                  BattleManager.cs      戦闘解決・パッシブ
│   │                  MarriageService.cs    婚姻
│   │                  ScoutService.cs       外様スカウト
│   ├── Naming/        NameGenerator.cs      名前キー生成（重複回避）
│   │                  NameResolver.cs       キー→日本語 解決
│   │                  NameCatalog.cs        名前プール
│   │                  NameTaxonomy.cs       文化圏・性別の分類
│   ├── Localization/  PhaseNameResolver.cs  フェーズ名 解決
│   ├── Timeline/      Prophecy.cs           予言レコード + 種別
│   ├── Unit/          Unit.cs               旅団員（不変・ステータス非保持）
│   │                  Equipment.cs          装備（5 大アイテム SoT）
│   └── Persistence/   SaveSerializer.cs     状態⇄JSON（純粋）
│                      SaveManager.cs        ★ファイルI/O（Godot依存・テスト対象外）
│
├── Autoload/  ☆ Godot 依存の外殻（身体の中枢） ☆
│   └── ChronicleGlobal.cs    常駐 SoT・全 API・シグナル・セーブ/ロード
│
├── UI/  ☆ Godot 依存の外殻（画面） ☆ ※すべてコードで動的構築（.tscn 不使用）
│   ├── GameDirector.cs       画面切替の司令塔（ヘッダー＋4画面の出し分け）
│   ├── TimelineUI.cs         年代記画面（予言3択）
│   ├── MarriageUI.cs         旅団組合画面（婚姻・スカウト・家系図）
│   ├── FormationUI.cs        大隊編成画面（※名簿一覧のみ）
│   └── BattleResultUI.cs     戦闘結果画面（ラストヒット解決＋演出）
│
├── Config/
│   └── localization_ja.json  全日本語テキストの辞書（唯一の翻訳元）
│
└── Tests/  ✅ xUnit 単体テスト（Core のみを対象）
    └── （詳細はセクション 4）
```

**データフローの全体像**:

```
  ┌─────────────┐   呼ぶ    ┌──────────────────┐   委譲   ┌─────────────┐
  │   UI 層      │ ───────▶ │   Autoload       │ ──────▶ │   Core 層    │
  │ (Godot依存)  │          │ ChronicleGlobal   │          │ (純粋ロジック) │
  │             │ ◀─────── │ (唯一の真実SoT)   │ ◀────── │             │
  └─────────────┘ シグナル  └──────────────────┘ 新レコード └─────────────┘
                  で通知                ↑↓                    ↑
                              lock で状態を保護        Random は引数注入で再現可能
```

この分離のご利益は明確です：**戦闘計算や経済ロジックといった「ゲームの心臓部」を、Godot を一切起動せずにテストできる**。`dotnet test` を回せば、画面なしで「狙撃兵の二連撃ダメージは 220 か？」を機械的に検証できます。

---

# 3. 各システム仕様のディテール棚卸し

> **【ひとことで言うと】** ここでは「実際にどう計算しているか」を式とエッジケース込みで全部開示する。すでに動くロジックの全仕様書。

## 3-1. 💰 経済システム（`PointsEconomy`）

すべての消費（スカウト・結婚・将来の成長/装備強化）が **単一通貨「ポイント」**を共有する財布です。状態は **3 つの数値のみ**：現在残高 / 累計獲得 / 累計消費。

### 収入源（3 経路）

| 経路 | メソッド | 計算式 | 例 |
|---|---|---|---|
| ① タイムスキップ年次収入 | `EarnFromTimeSkip(years)` | `years × 1pt` | 4 年スキップ → +4pt |
| ② 敵撃破報酬 | `EarnFromKill(enemyLevel)` | `floor(enemyLevel × 1.5)` | Lv10 撃破 → +15pt |
| ③ 特殊効果（強欲の古銭等） | `EarnDirect(delta)` | 任意の正数を直接加算 | 古銭 Lv5 LH → +1pt |

### 消費と残高不足時の挙動

- `CanAfford(cost)` … 残高 ≥ cost を純粋判定（true/false）。
- `SpendPoints(cost)` … **残高不足なら `InvalidOperationException` を送出して拒否**（マイナス残高は構造的に発生しない）。

### エッジケース

- 負の年数・負のコストはすべて `ArgumentOutOfRangeException`。
- `EarnDirect` に負数を渡すと例外（減算には必ず `SpendPoints` を使う、という規律の強制）。

## 3-2. 🔮 予言タイムライン（`TimelineEngine` ＋ `Prophecy`）

毎ターン **必ず 3 つの予言**が提示され、プレイヤーは 1 つだけ選びます。

### 予言の中身（`Prophecy`）

各予言は `Id` / `Kind`（種別）/ `SkipYears`（2〜4 年）/ `Value`（効果量）/ `DescriptionKey`（表示キー）を持ちます。種別は 5 つ：

| 種別 (`ProphecyKind`) | 意味 | `Value` の意味 |
|---|---|---|
| `RewardPoints` | 安全にポイント獲得 | 獲得ポイント数 |
| `Battle` | 敵との戦闘が発生（戦死リスクあり） | 敵レベル/強さ係数 |
| `ScoutReward` | 新ユニットが加入 | 加入人数 |
| `EquipmentDrop` | 装備がドロップ | 装備レベル (1〜5) |
| `Rest` | 休息（戦闘も報酬もなし） | 休息効果の強度 |

### リロール不可の「構造的」保証 ★設計の妙

「予言は引き直せない」というルールを、**コメントやチェックではなく型の構造そのもので保証**しています：

- `TimelineEngine` は **次ターンの予言を保持するフィールドを一切持たない**。
- 次の予言は `AdvanceToNextTurn()` を通してしか生成できず、その瞬間に過去の選択肢は破棄される。
- つまり「未来は、現在の選択を確定するまで存在しない」が物理的に成立する。

### 暫定ジェネレータと保留年数

- 現状の予言生成は `DefaultGenerator`（種別を均等巡回する**暫定実装**）。将来 `ProphecyMaster` に置換予定。
- プレイヤーが選んだ予言の `SkipYears` は、即座には消費されず **`_pendingGenerationSkipYears`（保留年数）**として `ChronicleGlobal` に退避され、ループ幕引きまで持ち越されます（→ 3-3、コア思想の中核）。

## 3-3. 👥 ロスター ＆ 世代交代（`Unit` / `RosterLifecycle`）

### C# 版 `Unit` が「ステータスも HP も持たない」理由 ★TS 版との最大の差分

TypeScript 版の `Unit` は HP・攻撃力・速度などをすべて自前で保持していました。**C# 版の `Unit` は意図的にそれらを捨てています。**

`Unit` が持つのは「**個体の履歴情報だけ**」です：

| 保持する | 保持しない（→ `JobMaster` が動的解決） |
|---|---|
| Id / Job（ジョブID） / Age / MaxAge（寿命） | HP / 攻撃力 / 速度 / 各種パッシブ値 |
| Level (1〜3) / Origin（血統） | 現在 HP（戦闘中の一時値） |
| MainEquipment / BattleAffinity（好感度） | |
| FirstNameKey / LastNameKey / IsDead | |

**なぜか**: ジョブの能力値は全個体で共通であり、`Unit` ごとに複製して持つのは無駄でバグの元。`unit.Job` から `JobMaster.All[Job].Stats` を引けば常に最新・唯一の値が得られる。これにより `Unit` は軽量・不変・「経歴書」に徹し、戦闘数値は SoT（`JobMaster`）に一元化される。

### 世代交代の処理（`RosterLifecycle.AdvanceGeneration`）

ループ幕引きで呼ばれる純粋関数。手順：

1. **加齢**: 生存者だけを `SkipYears` ぶん加齢（`WithAgeProgress`）。戦闘死者は加齢しない（据え置き）。
2. **仕分け**: `unit.IsRemovedFromRoster`（= 戦闘死 `IsDead` または 寿命到達 `Age >= MaxAge`）で **現役（Surviving）／離脱（Departed）**に分割。
3. **集計**: `AppliedSkipYears` / `SurvivingCount` / `DepartedCount` を返す。

> **完全ロスト仕様**: 離脱した者は二度と現役名簿に戻りません。寿命でも戦死でも不可逆。装備も同時に失われます（古き者は静かに退場する）。

### エッジケース（テストで固定済み）

- 寿命ちょうど（59 歳 +1 年 = 60 = MaxAge）でも離脱（`>=` 判定）。
- 戦闘死ユニットは `skipYears=0` でも離脱側へ（戦闘後クリーンアップを兼ねる）。
- 順序保持：現役・離脱とも元の名簿順を厳密に維持。
- 負の年数 → `ArgumentOutOfRangeException`、null 名簿 → `ArgumentNullException`、空名簿 → 空結果。

### シグナル発火の厳密な順番 ★

年送り（`Battle → Chronicle`）が起きると、`ChronicleGlobal` は **以下の順序を死守**して発火します：

```
  RosterChanged ─▶ EconomyChanged ─▶ TimelineChanged ─▶ PhaseChanged
  （名簿確定）     （残高確定）       （予言確定）       （画面切替）
  └──────────── データ系を先に全部確定 ───────────┘   └─ 最後に画面を切る
```

これにより、画面切り替えのトリガー（`PhaseChanged`）が走る時点で、新しい名簿・残高・予言が **100% 確定済み**であることが保証されます。

## 3-4. ⚔️ 戦闘解決 ＆ ジョブパッシブ（`BattleManager` / `JobMaster`）

### 全 8 ジョブの能力値 SoT（`JobMaster`）

| ジョブ | MaxHp | 速度 | 前衛攻撃 | 後衛攻撃 | 特殊パッシブ |
|---|---|---|---|---|---|
| 鉄壁騎士 (IronWallKnight) | 250 | 10 | 50 | 10 | 大隊守護 10 / 分隊守護 15 |
| 重装歩兵 (HeavyInfantry) | 300 | 15 | 70 | 20 | 分隊守護 10 |
| 旗手 (StandardBearer) | 150 | 20 | 30 | 30 | 分隊守護 5 / 号令 40 |
| 戦術官 (Tactician) | 120 | 35 | 20 | 20 | 号令 20 |
| 衛生兵 (Medic) | 100 | 25 | 10 | 10 | ターン終了時 自分隊回復 30 |
| 狙撃兵 (Sniper) | 80 | 40 | 20 | 90 | **連続攻撃（二連撃）** |
| 呪術師 (Sorcerer) | 40 | 15 | 10 | 120 | （なし・純粋火力） |
| 斥候 (Scout) | 90 | 60 | 40 | 40 | （なし・最速） |

### パッシブ ① 行動順構築（`BuildInitiativeOrder`）

実効速度の高い順に行動。**同速のときは味方が敵より先**（安定ソートの味方優先タイブレーク）。

- 実効速度 = `自身の速度 + 号令ブロードキャストによる速度ボーナス`

### パッシブ ② 号令ブロードキャスト（InitiativeBuff — 戦術官・旗手）

号令持ちは **自分以外の全生存者**に速度と攻撃を配ります（自分には配らない・複数いれば加算）：

- 速度ボーナス = 配り手の **速度ステータス**
- 攻撃ボーナス = 配り手の **号令ステータス（InitiativeBuff）**

**実例（テストで固定済み）**:

```
 狙撃兵 単独           : 実効速度 = 40
 狙撃兵 + 戦術官1人    : 実効速度 = 40 + 35(戦術官の速度) = 75
                         攻撃ボーナス = 20(戦術官の号令)
 狙撃兵 + 戦術官 + 旗手: 速度ボーナス = 35 + 20 = 55 → 実効速度 95
                         攻撃ボーナス = 20 + 40 = 60
```

### パッシブ ③ 連続攻撃（ConsecutiveStrike — 狙撃兵）

行動順の **先頭に立ったときだけ** 攻撃が 2 回になります。

- 攻撃ダメージ = `(その列の攻撃力 + 攻撃ボーナス) × 攻撃回数`

**実例（テストで固定済み）**:

```
 狙撃兵が先頭 + 戦術官の号令あり : (後衛90 + 20) × 2回 = 220
 狙撃兵が単独で先頭             : (後衛90 +  0) × 1回 =  90  ※先頭でも号令なしなら...
   └ 注: 連続攻撃の発動条件は「先頭」かつ「ConsecutiveStrike 保持」。
        斥候(速度60)が先頭なら連続攻撃なしで 1 回攻撃。
```

### パッシブ ④ ダメージ軽減（鉄壁騎士など）

被ダメージは **2 段階の守護**で軽減されます：

```
 最終被ダメージ = max(1, baseDamage − 大隊守護 − 分隊守護)
```

- **大隊守護（BattalionDefense）**: 前衛配置時のみ発動。大隊**全員**の被ダメを軽減。
- **分隊守護（SquadDefense）**: その分隊**自身**の被ダメを軽減。

**実例（鉄壁騎士：大隊守護10 / 分隊守護15、テストで固定済み）**:

```
 後衛分隊が被弾（鉄壁は前衛にいる）: 100 − 10(大隊) − 0(分隊) = 90
 鉄壁のいる前衛分隊が被弾          : 100 − 10(大隊) − 15(分隊) = 75
 鉄壁+旗手が同分隊（守護 15+5=20） : 軽減 20
 軽減しすぎても最低保証            : 5 − 25 = max(1, -20) = 1  ← 最低 1 ダメージは必ず通る
 鉄壁が戦闘不能                    : 守護 0 / 0 → 100 そのまま
```

### パッシブ ⑤ 継続回復（衛生兵）

ターン終了時、衛生兵は自分隊を回復します。回復は HP 上限でクランプ：

```
 ApplyHealClamped(currentHp, maxHp, heal) = min(maxHp, currentHp + heal)
```

**実例（衛生兵：回復30、テストで固定済み）**:

```
 衛生兵 + 呪術師 が同分隊 : 合計回復 30（呪術師は回復0）
 衛生兵 2 人             : 合計回復 60
 回復のクランプ          : (HP5/40, 回復30) → 35  /  (HP30/40, 回復30) → 40 で頭打ち
 負の回復量              : ArgumentOutOfRangeException
```

### ラストヒット（LH）解決（`ExecuteLastHit`）

「誰がトドメを刺したか」で報酬が変わる、ハクスラの脳汁ポイント：

1. **ユニット成長**: Lv<3 なら +1。Lv3 ならオーバーフロー（経験値は無駄に流れる）。
2. **装備強化**: 装備 Lv<5 なら確定 +1。
3. **Lv5 装備の運命**: 50% で砕け散る（完全ロスト）／ 50% で生存。
4. **強欲の古銭 Lv5**: 100% 破壊だが、引き換えに +1pt を強奪（特殊効果）。

戦闘後は相互好感度（`AccumulateMutualBattleAffinity`）も蓄積し、自然婚姻（3-5）の種になります。

## 3-5. 💍 婚姻 ＆ スカウト（`MarriageService` / `ScoutService`）

### 手動婚姻のコスト計算

```
 父コスト = JobMaster.CalculateTargetRating(父.Job) × 婚姻コスト倍率
 母コスト = 同上 for 母
 必要ポイント = ceil( (父コスト + 母コスト) / 20 )    ※ CostDivisor = 20
```

例：両者の Rating が 145 同士・倍率 1.0 なら `ceil((145+145)/20) = 15pt`。

### 自然婚姻（コスト 0 ＝「タダ結婚」）★

双方向の好感度（`BattleAffinity`）が **ともに 150 以上**に達したペアは、**コスト 0 で結婚できる**特権を得ます（純愛ルート）。`NaturalMarriageThreshold = 150`。

### 子供への遺伝ルール

生まれる子（`Unit`）は：

- **Level**: 必ず 1 から。
- **ジョブ**: `OverrideJob` 指定がなければ **父か母から 50% / 50% で乱数継承**。
- **血統（Origin）**: 父か母の文化圏を **50% / 50%** で継承（系譜の追跡に使う）。
- **名前**: 継承した文化圏のプールから、重複しないキーを自動払い出し。
- **好感度・装備**: 空（まっさらな新生児）。

### エッジケース

- 死亡したユニット同士は結婚不可（`InvalidOperationException`）。
- 父＝母（同一ユニット）は `ArgumentException`。
- `ChronicleGlobal` 側はこれらの例外を握り潰して `null` を返す（UI を落とさないヌル安全）。

### 外様スカウト（`ScoutService`）

ポイントを払って **血縁のない傭兵（外様）**を 1 名即採用：

- 年齢 16〜28 歳（成人・即戦力）／ 寿命 55〜75 歳（個体差）。
- ジョブ・文化圏・名前はすべて乱数。Level 1・装備なし・好感度なし・親なし。
- 残高不足・負コストは **例外を投げず `null`**（呼び出し側の安全契約）。

## 3-6. 📛 命名 ＆ ローカライズ（`NameGenerator` / `NameResolver` / `PhaseNameResolver`）

### 文化圏（Origin）の概念

ユニットは 3 つの文化圏のいずれかに属します：**Japanese（日本）/ European（ヨーロッパ）/ Classical（古典）**。名前プールが文化圏ごとに分かれ、血統の系譜追跡にも使われます。手動入団・旧セーブ互換のデフォルトは `European`。

### 名前プール枯渇時の「称号付き複合キー」フォールバック ★巧妙な堅牢化

同じ文化圏×性別の名前を使い切ったら（プール枯渇）、エラーにせず **称号キーと名前キーを `@` で連結した複合キー**を払い出します：

```
 通常キー : "name-japanese-male-007"
 複合キー : "title-003@name-japanese-male-007"   ← プール枯渇時のフォールバック
              └─ 称号 ─┘ └──── 名前 ────┘
```

`NameResolver` はこの `@` を自動でパースし、**「称号 ＋ 名前 ＋（あれば）姓」**の順で 1 つの美しい日本語氏名に連結します。

```
 "title-003@name-japanese-male-007" + "family-japanese-012"
   ↓ 辞書を引いて連結
 「鉄血の」＋「タケル」＋「ヤマト」 = 「鉄血のタケルヤマト」
```

称号 × 名前の全組み合わせすら尽きた場合のみ例外（最大 10,000 試行 — 事実上到達不能）。`Jr.` や `II 世` のような記号方式は仕様で不採用、称号方式のみ。

### 解決の堅牢性

- **未知キーは例外を投げず、生のキー文字列をそのまま返す**（フォールバック）。
- → 辞書への登録漏れがあっても画面は落ちず、「どのキーが未登録か」が画面上で一目で分かる（デバッグ性）。
- 純粋層（`NameResolver`）は `Godot.*` を一切参照しない → xUnit で完全検証可能。res:// の読み取りは `ChronicleGlobal` の責務に分離。

## 3-7. 🔁 進行状態マシン（`GamePhaseFlow`）

4 大フェーズの **一方通行・不可逆な循環**：

```
 Chronicle ──▶ Guild ──▶ Formation ──▶ Battle ──┐
 (年代記)      (旅団組合)  (大隊編成)    (戦闘)    │
     ▲                                          │
     └──────────────（次世代へ）─────────────────┘
```

- `Next(current)` … 各フェーズの「次」はただ 1 つ（純粋関数）。
- `CanTransition(from, to)` … **後退・飛び越し・自己遷移はすべて `false`**（絶対ガード）。
- `Slug()` … ASCII スラッグ（"chronicle" 等）を払い出し、ローカライズキー組み立てと画面ノード名に使う。
- `Battle → Chronicle` だけが特別で、ここがループ幕引き＝年送り発火点（3-3）。

## 3-8. 💾 永続化（`SaveManager` / `SaveSerializer`）

### ⚠️ 重要な事実確認：暗号化は「されていない」

> セーブデータは **暗号化されていません**。`WriteIndented = true` の**人間が読める整形済み JSON**として保存されます（`user://save_data.json`）。これは可読性・デバッグ性・スキーマ並べ替え耐性を優先した設計判断です。改ざん防止が必要になった段階で別途追加する余地があります。

### アトミック書き込み（破損耐性）★

代わりに、**クラッシュ・電源断でセーブが壊れない**ことを保証する堅牢な書き込み手順を実装しています（Temp-swap + バックアップローテーション）：

```
 1. path.tmp に全文を書き切る + Flush
      → 途中でクラッシュしても汚れるのは .tmp だけ。本ファイルは無傷。
 2. 既存の本ファイルを path.bak へ退避（ローテーション）
      → クラッシュしても本ファイルは旧バージョンのまま正常。
 3. 本ファイルを削除 → path.tmp を本ファイルへリネーム
      → クラッシュしても .bak が旧バージョンを保持。

 ★不変条件: どの瞬間にクラッシュしても
   「本ファイルが正常」か「バックアップが正常」のどちらかが必ず成立する。
```

ロード時は本ファイル → 失敗したら自動で `.bak` へフォールバック。

### DTO マッピング方式と保存対象

- 不変レコードを直接シリアライズせず、**可変 DTO クラス**を経由（`record ⇄ DTO` 明示マッピング）。ドメインのリファクタがセーブ互換を壊さない疎結合を実現。
- **enum は文字列で保存**（"Sniper" 等）— 定義順を変えてもデータが壊れない。
- Guid キー辞書（好感度）は**文字列キーに正規化**して保存。
- `Version` フィールドでスキーマバージョニング（現 `1`）、`SavedAtUtc` で保存日時記録。

### ロード時の Chronicle 強制再開ルール ★

- **Random は保存しない**。ロード時に `ChronicleGlobal` が**新しい Random を再注入**する。
- ロード後の `CurrentPhase` は **常に `Chronicle` から再開**（フェーズ途中の中途半端な状態を持ち越さない）。
- 保留年数（`_pendingGenerationSkipYears`）もセーブ対象外（Chronicle で改めて設定されるため）。

---

# 4. 現状の実装状況マトリクス

> **【ひとことで言うと】** 「脳（ゲームルール）はほぼ完成、テストも厚い。だが、それを動かす身体（Godot の器）と、本物の戦闘シーンがまるごと無い。」

凡例：**🟢 実装完了（100% 稼働）** ／ **🟡 簡易実装（ガワのみ・TODO あり）** ／ **🔴 未実装（コードが 1 ミリも存在しない）**

## 4-1. Core 層（純粋ゲームロジック）

| システム | 状態 | 備考 |
|---|---|---|
| ポイント一元経済 (`PointsEconomy`) | 🟢 | 収入3経路・消費・例外処理すべて完備 |
| 予言タイムライン (`TimelineEngine`/`Prophecy`) | 🟢 | リロール不可を構造保証。ただし生成器は暫定 |
| ロスター/世代交代 (`Unit`/`RosterLifecycle`) | 🟢 | 加齢・完全ロスト・順序保持・集計まで完備 |
| ジョブ SoT (`JobMaster`) | 🟢 | 全 8 ジョブの能力値・パッシブ・Rating 完備 |
| 戦闘パッシブ解決 (`BattleManager`) | 🟢 | 行動順・号令・二連撃・軽減・回復・LH 完備 |
| 婚姻 (`MarriageService`) | 🟢 | 手動/自然婚姻・遺伝・コスト計算完備 |
| 外様スカウト (`ScoutService`) | 🟢 | 残高検証・生成・追加・ヌル安全完備 |
| 装備 (`Equipment`) | 🟢 | 5 大アイテム・レベル乗算・好感度倍率完備 |
| 命名/血統 (`NameGenerator`/`NameResolver`) | 🟢 | 重複回避・称号フォールバック・解決完備 |
| フェーズ状態マシン (`GamePhaseFlow`) | 🟢 | 一方通行循環・絶対ガード完備 |
| 永続化シリアライザ (`SaveSerializer`) | 🟢 | DTO 方式・往復等価・バージョニング完備 |
| **本格戦闘シミュレータ** | 🔴 | **下記「最大の欠落」参照** |
| 予言生成マスター (`ProphecyMaster`) | 🔴 | 現状 `DefaultGenerator`（均等巡回）の暫定のみ |
| 敵生成・敵スケーリング | 🔴 | TS 版 `makeTrialEnemy` 相当が皆無 |
| Affix（接尾効果）システム | 🔴 | `AffixKeys` は保持するが解決マスター無し |
| 装備ドロップ予言の効果ハンドラ | 🔴 | `EquipmentDrop` 種別はあるが処理が無い |
| 敵撃破収入の戦闘フロー結線 | 🟡 | `EarnFromKill` は実装済だが戦闘に未接続 |

## 4-2. Autoload 層（常駐 SoT）

| 機能 | 状態 | 備考 |
|---|---|---|
| `ChronicleGlobal` 状態保持・全 API | 🟢 | 初期化/LH/予言選択/婚姻/スカウト/フェーズ/年送り/ローカライズ/セーブ・ロード すべて結線済み |
| スレッド安全・ヌル安全ガード | 🟢 | lock 規律・SafeEmit・例外握り潰しを完備 |
| 年送り結線（1世代=1周） | 🟢 | `AdvanceGenerationLocked` で加齢→収入→予言再生成を一括適用 |
| **新規ゲームのブートストラップ** | 🔴 | **`Initialize()` を呼ぶ起動エントリが存在しない（重大）** |
| 初期ロスター生成 | 🔴 | 開始時の旅団を組む処理が無い |

## 4-3. UI 層（すべてコードで動的構築・`.tscn` 不使用）

| 画面 | 状態 | 備考 |
|---|---|---|
| `GameDirector`（司令塔） | 🟡 | フェーズ切替＋ヘッダーは動く。ただし `Initialize()` を呼ばない |
| `TimelineUI`（年代記） | 🟡 | 予言3択・残高・ターン表示は機能。ジョブ/予言名ハードコード |
| `MarriageUI`（旅団組合） | 🟡 | 婚姻・スカウト・家系図は機能。ジョブ名ハードコード |
| `FormationUI`（大隊編成） | 🟡 | **名簿一覧のみ。3×3 V字配置は将来へ明示保留** |
| `BattleResultUI`（戦闘結果） | 🟡 | LH 解決＋4演出は機能。ジョブ/アイテム名ハードコード |
| **本格戦闘画面**（ターン進行・敵カード・攻撃予告・ローテーション可視化） | 🔴 | 皆無 |
| UnitIcon / ドット絵アセット | 🔴 | 未移植 |

## 4-4. Tests 層（xUnit・Core のみ対象）

| テストファイル | 状態 | 対象 |
|---|---|---|
| `GamePhaseFlowTests` | 🟢 | フェーズ遷移ルール |
| `PhaseNameResolverTests` | 🟢 | フェーズ名解決 |
| `BattlePassiveTests` | 🟢 | ジョブパッシブ（二連撃・軽減・回復）★今期移植 |
| `PointsEconomyTests` | 🟢 | 経済の収入・消費・例外 |
| `RosterLifecycleTests` | 🟢 | 世代交代・完全ロスト ★今期移植 |
| `ScoutServiceTests` | 🟢 | スカウト残高検証・生成 |
| `NameGeneratorTests` | 🟢 | 名前生成・称号フォールバック |
| `NameResolverTests` | 🟢 | キー→日本語解決・複合キー分解 |
| `SaveSerializerTests` | 🟢 | 保存/復元の往復等価性 |
| `SampleData`（共有ファクトリ） | 🟢 | テスト用決定論的状態 |
| `MarriageService` のテスト | 🔴 | 未作成 |
| `TimelineEngine` のテスト | 🔴 | 未作成 |
| `Equipment` のテスト | 🔴 | レベル乗算・好感度倍率の検証なし |
| `BattleManager` の LH 系テスト | 🔴 | パッシブ系のみ。LH（破壊/強奪）未検証 |
| Autoload / UI のテスト | ⚪ | Godot 依存のためテスト対象外（静的検証で代替） |

> **検証手段の現状**: `dotnet` ツールチェーンが当環境に未導入のため、テストは**静的・論理検証のみ**で担保（実行はしていない）。

## 4-5. Localization 層

| 項目 | 状態 | 備考 |
|---|---|---|
| `localization_ja.json` の網羅性 | 🟢 | phases/passives/squadRows/effectKinds/effectScopes/jobs/items/names/ui/marriage を完備 |
| `NameResolver`/`PhaseNameResolver` の消費 | 🟢 | 名前・フェーズ名は JSON から解決 |
| **UI のジョブ/アイテム名解決** | 🟡 | JSON に `jobs`/`items` は存在するが、**UI は引かずにハードコード**（下記） |
| 予言の `DescriptionKey` の JSON 登録 | 🔴 | 予言の説明文がまだ辞書に無い |
| ジョブ/アイテム名解決サービス | 🔴 | `LocalizationService` 相当が未実装 |

## 4-6. 🚨 TypeScript 版にあって C# 版に「ごっそり欠けている」最大の要素

これが本プロジェクト最大のギャップです。**ゲームの花形である「戦闘」が、まだ本物ではありません。**

| TS 版に存在 | C# 版の現状 |
|---|---|
| `BattleSimulator`（ターン進行エンジン全体） | 🔴 皆無。`BattleManager` は「1 アクションの解決」止まり |
| `Squad` / `Brigade` / `Enemy` クラス | 🔴 皆無。分隊・大隊・敵の概念がコード上に無い |
| 3×3 V字盤面配置・`GridPlacement` | 🔴 皆無。`FormationUI` は名簿一覧のみ |
| 分隊ローテーション（CW/CCW swap） | 🔴 皆無 |
| 攻撃予告（`AttackIntent`）・先読み | 🔴 皆無 |
| 敵生成・敵スケーリング（年次強化） | 🔴 皆無 |

> **意図的に置換・不要化したもの**（これは欠落ではなく設計判断）:
> - API レイヤ（Hono）→ `ChronicleGlobal` Autoload が代替
> - React フロントエンド → Godot UI が代替
> - `Unit` の戦闘ステータス保持 → `JobMaster` の動的解決へ移譲

## 4-7. 細かい品質課題（包み隠さずリストアップ）

1. **🟡 UI のジョブ名ハードコード**: `BattleResultUI.FormatJob` / `MarriageUI.FormatJob` / `FormationUI.FormatJob` が「鉄壁騎士」等を日本語直書き（各コードに TODO 明記済み）。`localization_ja.json` の `jobs` セクションは既にあるので引くだけで解消可能。
2. **🟡 UI のアイテム名ハードコード**: `BattleResultUI.FormatItem` が「🛡️ 誓いの聖剣」等を直書き。`items` セクション完備済みなので置換可能。
3. **🟡 予言種別ラベルのハードコード**: `TimelineUI.FormatProphecyKindLabel/Icon` が日本語直書き。
4. **🟡 コメント陳腐化**: `TimelineUI.cs` 冒頭コメント（7〜8 行目）が「予言選択でタイムスキップ＋収入＋再生成を行う」と記載しているが、年送りをループ幕引きへ移管した現在は**事実と不一致**。要修正。
5. **🟡 暫定予言生成器**: `TimelineEngine.DefaultGenerator` はバランス未調整の均等巡回。`ProphecyMaster` への置換が前提。

> ❗ 上記 1〜3 は **憲法①（日本語ハードコード禁止）に抵触する唯一の領域**です。Core 層は完全準拠していますが、UI 層に「TODO 付きの暫定ハードコード」が残っています。

---

# 5. プレイアブル化へのタイムライン ＆ 最短ロードマップ

> **【ひとことで言うと】** 中身（ロジック）は立派なのに、それを起動する「箱」が存在しない。`project.godot` も `.csproj` も起動スイッチも無いので、今は Godot で開くことすらできない。逆に言えば、この「箱」を作れば一気に動き出す。

## 5-1. 🚨 重大な発見：「脳だけがあって、肉体（器）がない」

コアロジックは堅牢に積み上がっています。しかし、それを **実行可能なゲームにする外殻が完全に欠落**しています：

| 必要なもの | 現状 |
|---|---|
| `project.godot`（Godot プロジェクト定義） | ❌ 存在しない |
| 本体ゲームの `.csproj`（ビルド定義） | ❌ 存在しない（唯一あるのは `Tests/ChronicleKnights.Tests.csproj` のみ） |
| `.sln`（ソリューション） | ❌ 存在しない |
| メインシーン `.tscn` | ❌ 存在しない（UI は全部コード構築） |
| Autoload 登録（`/root/ChronicleGlobal`） | ❌ 未登録 |
| **新規ゲームの起動スイッチ**（`Initialize()` 呼び出し） | ❌ **誰も呼ばない** |

**今 Godot エディタで `generated_csharp/` を開いても、プロジェクトとして認識されません。** これがプレイアブル化の唯一にして最大のボトルネックです。

さらに致命的なのが最後の項目です。`GameDirector._Ready()` は `LoadLocalization()` こそ呼びますが、**`ChronicleGlobal.Initialize()` を一切呼びません**。`Initialize()` が呼ばれないと `IsInitialized = false` のままで、**全ての API が no-op（何もしない）になり、全画面が空っぽ**になります。つまり「箱」を作っても、起動スイッチを押す処理が無ければゲームは始まりません。

## 5-2. マウスでループを回せるようになるまでの手順（ステップバイステップ）

| # | ステップ | 何をするか | 目安 |
|---|---|---|---|
| 0 | **前提** | Godot 4.x の **.NET（Mono）版**をインストール | — |
| 1 | **`project.godot` 作成** | プロジェクト定義・.NET 有効化・メインシーン指定・Autoload 登録欄を記述 | 0.25 日 |
| 2 | **本体 `.csproj` 作成** | `Godot.NET.Sdk` 採用・`net8.0` ターゲット・`Core`/`Autoload`/`UI` を含める。`.sln` で Tests と束ねる | 0.25 日 |
| 3 | **`Main.tscn` 作成** | ルートに `Control` を置き `GameDirector` スクリプトをアタッチ → 実行シーンに設定 | 0.25 日 |
| 4 | **Autoload 登録** | `ChronicleGlobal` を `/root/ChronicleGlobal` に登録（`GetNodeOrNull` が前提とするパス） | 0.1 日 |
| 5 | **🔑 起動スイッチ実装** | **最重要**。初期ロスター（固定 or `ScoutService` で数名）を組み、`ChronicleGlobal.Initialize(roster, economy, timeline, rng)` を呼ぶ起点を追加（タイトル画面 or `GameDirector._Ready` 冒頭） | 0.5 日 |
| 6 | **取り込み & ビルド** | Godot に取り込み → `dotnet build`（エディタ経由）成功確認 → 実行 | 0.5〜1 日 |
| 7 | **ループ実プレイ** | 年代記（予言選択）→ 旅団組合（婚姻/スカウト）→ 編成（名簿確認）→ 戦闘（LH 解決）→ 年送りで年代記へ、をマウスで一巡 | 0.25 日 |

### ⏱ 最短到達見通し

**約 1.5〜2 日**で「マウスでポチポチと 1 周回る**スケルトンループ**」がエディタ上で動きます。

### ⚠️ 「プレイアブル」の正直な定義

上記で動くのは **骨格ループ**です：予言選択 ＋ 婚姻/スカウト ＋ 名簿一覧 ＋ ラストヒット解決 ＋ 年送り。

**実際の戦闘（3×3 V字陣形・ターン進行・敵・ローテーション・攻撃予告）はまだ動きません。** Battle フェーズは現状「誰がトドメを刺したか」を選ぶだけの画面です。したがって「プレイアブル＝ゲーム完成」ではなく、**「アーキテクチャがエディタ上で端から端まで通電し、世代交代の循環を手で体験できる」段階**だと理解してください。これでも価値は絶大です — 積み上げた全設計が初めて「動く実物」として検証できます。

---

# 6. 今後の推奨開発フェーズ

> **【ひとことで言うと】** まず「箱」を作って骨格を動かす（最小リスク・最大の検証価値）。次に翻訳結線で唯一の規約違反を消す。そこから初めて、最大の難所である「本物の戦闘」に着手する。

```
Phase 1  Godot 土台整備 + 起動スイッチ   ◀ 即・最優先（1.5〜2日）
   │       目的: スケルトンループを動かし、全アーキテクチャを実機検証
   ▼
Phase 2  UI ローカライズ結線            （0.5〜1日）
   │       目的: ジョブ/アイテム/予言名のハードコードを JSON 解決に置換
   │             → 憲法①違反（UI 層）を根絶 ＋ コメント陳腐化も是正
   ▼
Phase 3  戦闘コアの設計判断と移植        （数日〜・最大の難所）
   │       決断: (A) TS版 BattleSimulator をフル移植（Squad/Enemy/3×3/
   │             ローテ/予告の完全再現） vs (B) 現状の結果解決型を軸に
   │             敵生成・EarnFromKill 結線・戦死マーキングだけ足す軽量版
   ▼
Phase 4  Formation V字配置 + 戦闘画面     （体験の本丸）
   │       3×3 V字グリッド配置 UI・ターン進行可視化・敵カード・攻撃予告
   ▼
Phase 5  仕上げ
           ドット絵/UnitIcon・セーブ/ロード UI ボタン・ProphecyMaster・
           Affix システム・残るテスト拡充（Marriage/Timeline/Equipment/LH）
```

## 推奨：直近の優先タスク 3 つ

### ① 【最優先】Phase 1 — Godot 土台整備 ＋ 起動スイッチ

最も価値が高く、リスクが最も低い一手。`project.godot` / `.csproj` / `.sln` / `Main.tscn` / Autoload 登録を整え、`Initialize()` を呼ぶブートストラップを追加します。これにより**積み上げた全アーキテクチャがエディタ上で初めて通電**し、設計の正しさを実機で検証できます。スケルトンでも「自分の手でループが回る」体験は、その後の判断材料として極めて大きい。

### ② Phase 2 — UI ローカライズ結線

`localization_ja.json` の `jobs`・`items` セクションは既に存在します。UI の `FormatJob`/`FormatItem`/`FormatProphecyKind` のハードコードを解決サービス経由に置換し、**UI 層に残る唯一の憲法違反（日本語ハードコード）を根絶**。ついでに `TimelineUI` の陳腐化コメントも是正。小さく確実な品質向上です。

### ③ Phase 3 入口 — 戦闘コアの設計判断

最大の未移植領域。**(A) フル移植** か **(B) 軽量版** かの意思決定が、Phase 4 の UI 規模を大きく左右します。**Phase 1 でスケルトンを実際に触ってから (A)/(B) を決める**のが手戻り最小です。

---

## ✅ 総括

| 観点 | 評価 |
|---|---|
| **コアロジック（脳）** | 🟢 TS 版の主要システムをほぼ移植完了。不変設計・ロック規律・単方向フロー・年送り結線・パッシブ厳密検証まで堅牢 |
| **テスト** | 🟢 Core の主要 9 系統を xUnit でカバー（静的検証で担保） |
| **Godot 外殻（身体）** | 🔴 プロジェクト土台・起動スイッチが完全欠落 — **唯一にして最大のボトルネック** |
| **本物の戦闘** | 🔴 シミュレータ・盤面・敵が未移植 — 最大の作り込み領域 |
| **次の一手** | ▶ **Phase 1（土台整備）を強く推奨**。数日以内に「自分の手で 1 世代を回す」感触を得て、戦闘実装の優先判断を明確化する |

私たちは「壊れない脳」を完成させました。次に必要なのは、その脳を宿す「箱（器）」を組み立て、最初の鼓動を与えることです。

---

*— 本レポートは `generated_csharp/` 全 37 ファイル / 約 8,793 行のスキャンに基づき、コードの実態のみを記録したものです。提案・将来構想はセクション 5・6 にのみ含まれます。*
