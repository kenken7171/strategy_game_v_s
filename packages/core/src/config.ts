import gameSettings from "../../../config/game_settings.json";

export const MAX_UNITS_PER_SQUAD: number = gameSettings.max_units_per_squad;
export const MAX_ENEMY_ACTION_LOOP: number = gameSettings.max_enemy_action_loop;
